# Security Policy

## Reporting Vulnerabilities

If you discover a security vulnerability within `intervals.icu-mcp`, please send an email to the repository maintainer instead of opening a public issue.

All security issues will be promptly addressed.

## Sensitive Information

Never commit your `INTERVALS_API_KEY` or personal credentials to git. Use environment variables or `.env` files (which are git-ignored by default).
